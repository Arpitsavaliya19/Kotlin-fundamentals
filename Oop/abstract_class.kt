/*--> if we create the class as an abstract class, then we inherit the class whcih is an abstract class so we
      implemet all the method of an abstract class in subclass*/



//using private modifire, we can use the variable of constructor
//within the class


//        using abstract class in kotlin, we do not abele o create the object of that abstract class


//>>>>>>>>>>>>>>>>------------------<<<<<<<<<<<<<<<<<<<
// if nay of the variable or a function in any class declare as the private then that variable and that method
// and variable is not access by another class
// we can also make a constructor as a protected or private


// if we create this function private, then other classes are not able to access


//---------------------------------------------------------------------------------------------------------------------



/*    --> here we declare start-driving class as an abstract class so we don't need to implement this function
          whenever we need this function we can implement this function*/


/* here the error my calss because of the we delcare the car class as an abstract class so we don't able to create an
 object of the car class here*//*


class mycar() : car(brand = "BMW") {
    init {
        // newcar()         // this throws an error because we create this method with private method

    }*/

