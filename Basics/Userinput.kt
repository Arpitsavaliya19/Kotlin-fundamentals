
fun main() {

    val userinput = readLine()

    val userinput1 = readLine()
    println("you are enter : $userinput")//we also convert the string input to any data type by using it's functioon

    println(userinput?.uppercase()) //we have to write ? after the variable because the user input also be a null


}