fun main() {
    println("hello world")

    //there are two types of the declaring variable in kotlin either specify the data type and it automatically
    // detact by the kotlin

    //here we didn't change the value of our variable, so we use val as a variable

    var age = 20
    println(" hii my name arpit savaliya i'm  $age years old")

    var newvariable: Int = 5 //if we make float variable , we must write f after the number in value of a variable
    //ex: var a : float = 5f like this  for double we don't need to write f in number

    println(newvariable)

    var a = 5
    println(a)

    // where ever you need to print use value of the variable, so use $ for it

    println("print the value of the newvariable: $newvariable")
    a = 1
    println("the value of the variable is now: $a")

    //basic operation

    val result = 15 + 8
    println(result)

    val d = 5
    val b = 7

    var c = d + b
    println(c)//(d+b)
    println("the value of the d+b is : ${d + b}")

    //also for basic operation you don't need to dec. variable

    println(15 + 13)
}













